defaults:
  - base
  - _self_

tasks:
  - "gsm8k_boxed"

vllm_config:
  max_sequences: 128
  max_batched_tokens: 2048
  tensor_parallel_size: 2
  data_parallel_size: 4

model_config:
  model_name: "test"
  use_best_ckpt: false
  step_number: -1 # take last step 

temperature: 0.7
top_p: 0.95
max_tokens: 8192
epochs: 1
